
<!-- Section 1 -->
<section class="w-full px-8 py-16 bg-gray-100 xl:px-8">
    <div class="max-w-5xl mx-auto">
        <div class="flex flex-col items-center md:flex-row">

            <div class="w-full space-y-5 md:w-3/5 md:pr-16">
                <p class="font-medium text-blue-500 uppercase">Cultivating People</p>
                <h2 class="text-2xl font-extrabold leading-none text-black sm:text-3xl md:text-5xl">
                    Changing The Way People Live and Work.
                </h2>
                <p class="text-xl text-gray-600 md:pr-16">Learn how to unleash your potential by working on things that matter to you. You focus on expressing your creativity and we'll take care of the rest.</p>
            </div>
            <div class="w-full mt-16 md:mt-0 md:w-2/5">
              <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>

              <div class="relative z-10 h-auto p-8 py-10 overflow-hidden bg-white border-b-2 border-gray-300 rounded-lg shadow-2xl px-7">
                  <h3 class="mb-6 text-2xl font-medium text-center">Join The Process</h3>
                  <%= render "devise/shared/error_messages", resource: resource %>
                    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "block w-full px-4 py-3 mb-4 border border-2 border-transparent border-gray-200 rounded-lg focus:ring focus:ring-blue-500 focus:outline-none" %>
                    <%= f.password_field :password, autocomplete: "current-password", class: "block w-full px-4 py-3 mb-4 border border-2 border-transparent border-gray-200 rounded-lg focus:ring focus:ring-blue-500 focus:outline-none" %>
                    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "block w-full px-4 py-3 mb-4 border border-2 border-transparent border-gray-200 rounded-lg focus:ring focus:ring-blue focus:outline-none" %>
                    <div class="block">
                        <%= f.submit "Sign Up", class: "w-full px-3 py-4 font-medium text-white bg-blue-600 hover:bg-violet-400 active:bg-violet-600 rounded-lg" %>
                    </div>
                    <p class="w-full mt-4 text-sm text-center text-gray-500">Already have an account?
                        <%= link_to "Sign In", new_session_path(resource_name), class: "text-blue-500 hover:text-violet-400 underline" %>
                    </p>
                </div>
              <% end %>
            </div>

        </div>
    </div>
</section>
