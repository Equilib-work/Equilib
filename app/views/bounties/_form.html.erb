<%= form_with(model: bounty) do |form| %>
  <% if bounty.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bounty.errors.count, "error") %> prohibited this bounty from being saved:</h2>

      <ul>
        <% bounty.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :type, style: "display: block" %>
    <%= form.number_field :type %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amount %>
  </div>

  <div>
    <%= form.label :wallet_id, style: "display: block" %>
    <%= form.text_field :wallet_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
